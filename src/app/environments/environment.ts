import { EnvironmentInterface } from 'src/app/shared/models/environment.interface';

export const environment: EnvironmentInterface = {
    production: false,
    apiUrl: 'https://api.fit-meetups.ru',
};
